namespace ScarIntel
{
    /*public class PersonFilter
    {
        private IEnumerable<Person> list;

        public PersonFilter(IEnumerable<Person> list)
        {
            this.list = list;
        }

        public PersonFilter Name(String s)
        {
            list = list.Where(p => p.name.ToLower().Contains(s.ToLower()));
            return this;
        }

        public PersonFilter Data(DateTime data)
        {
            list = list.Where(p => p.birthday.Date.Equals(data.Date));
            return this;
        }

        public PersonFilter Nacionalidade(String s)
        {
            list = list.Where(p => p.nacionality.ToLower().Contains(s.ToLower()));
            return this;
        }

        public List<Person> getFilter()
        {
            return list.ToList();
        }
    }*/
}